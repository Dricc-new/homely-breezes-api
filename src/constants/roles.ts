export enum ROLES {
    BASIC = 'BASIC',
    EDITOR = 'EDITOR',
    ADMIN = 'ADMIN'
}

export enum ACCESS_LEVEL {
    MAINTEINER = 40,
    OWNER = 50
}

export enum VISIBILITY {
    DRAFT = 'DRAFT',
    PUBLISH = 'PUBLISH'
}